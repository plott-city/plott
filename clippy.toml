cognitive-complexity-threshold = 30
too-many-arguments-threshold = 8
# implement pattern score aggregation
# optimize histogram generation with precomputed bins
# add prediction confidence threshold filter
# vectorize histogram computation
