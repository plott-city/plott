[workspace]
members = ["programs/*"]
resolver = "2"

[profile.release]
# Required by Anchor CLI 0.31: the build fails without overflow-checks on.
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
